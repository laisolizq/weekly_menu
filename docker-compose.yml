services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_DB: weekly_menu
      POSTGRES_USER: weekly_menu
      POSTGRES_PASSWORD: weekly_menu
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: